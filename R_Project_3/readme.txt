Viewing Leaflet Map:

I have not figured out how to display the leaflet map on github, but it's possible to view the raw html code and save it into a html file.  Then use a web browser to open the html file and view the leafmap.  
